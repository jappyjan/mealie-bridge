{
  "name": "mealie-bridge",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build ./src/index.ts --target=bun --outfile=dist/index.js",
    "push-docker-image": "docker build -t jappyjan/mealie-bridge . && docker tag jappyjan/mealie-bridge jappyjan/mealie-bridge:latest && docker push jappyjan/mealie-bridge:latest"
  },
  "dependencies": {
    "@elysiajs/swagger": "^0.7.4",
    "bring-shopping": "^1.5.1",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
